{
    "name": "ikoene/marvel-api-client",
    "license": "MIT",
    "type": "library",
    "description": "An API client to handle calls to the Marvel API.",
    "autoload": {
        "psr-4": {
            "ikoene\\Marvel\\": ["src/Marvel"]
        }
    },
    "require": {
        "guzzle/guzzle": "^3.9",
        "jms/serializer": "^1.3",
        "symfony/yaml": "^3.1",
        "doctrine/collections": "^1.3"
    },
    "require-dev": {
        "phpspec/phpspec": "^3.0"
    },
    "config": {
        "bin-dir": "bin"
    }
}
